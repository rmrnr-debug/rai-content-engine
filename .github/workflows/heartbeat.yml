name: Run Heartbeat

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes
  workflow_dispatch:

jobs:
  run-heartbeat:
    runs-on: ubuntu-latest

    steps:
      - name: Call Heartbeat API
        run: |
          curl -X GET https://rai-content-engine.vercel.app/api/heartbeat
